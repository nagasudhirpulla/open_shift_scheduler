@page
@model OSS.Web.Pages.ShiftRoles.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ShiftRole</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ShiftRole.RoleName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ShiftRole.RoleName)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ShiftRole.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
