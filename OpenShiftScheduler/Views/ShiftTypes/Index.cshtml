@model IEnumerable<OpenShiftScheduler.Models.AppModels.ShiftType>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.StartOffsetHrs)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.StartOffsetMins)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RoasterSequence)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ShiftSequence)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ColorString)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartOffsetHrs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartOffsetMins)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoasterSequence)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShiftSequence)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ColorString)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShiftTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShiftTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShiftTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
