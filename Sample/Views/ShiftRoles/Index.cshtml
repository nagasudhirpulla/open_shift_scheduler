@model IEnumerable<Sample.Models.ShiftRole>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShiftRoleId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShiftRoleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
